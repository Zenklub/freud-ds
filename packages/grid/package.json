{
  "name": "@freud-ds/grid",
  "version": "2.0.0",
  "description": "Zenklub - Freud DS Grid",
  "author": "Zenklub",
  "homepage": "",
  "license": "ISC",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && cp -r src/scss ./dist && cp package.json dist/package.json",
    "clean": "rm -rf dist"
  }
}
