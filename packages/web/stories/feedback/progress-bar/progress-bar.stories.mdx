import { Meta, Canvas, Story, ArgTypes } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import {
  FreudProgressBarComponent,
  FreudProgressBarModule,
} from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import { Default, BGColor, Indeterminate } from './ProgressBar.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudProgressBarModule],
    }),
  ]}
  parameters={{
    layout: 'centered',
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    },
  }}
  title="@freud-ds/web-components/Feedback/ProgressBar"
  component={FreudProgressBarComponent}
/>

<FreudHeaderComponent
  title="ProgressBar"
  description="Indicadores de progresso exibem a duração de um processo."
/>

## <a id="importing"></a>Import

```ts
import { FreudProgressBarModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos

<br />

### Default

<Canvas>
  <Story story={Default} />
</Canvas>

### Indeterminate

<Canvas>
  <Story story={Indeterminate} />
</Canvas>

### BG Color

<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor} />
</Canvas>

## <a id="api"></a>API

<ArgTypes of={FreudProgressBarComponent} />
