import { Meta, Canvas, Story, ArgTypes } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import {
  FreudInputTextAreaComponent,
  FreudInputTextAreaModule,
} from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Default,
  BGColor,
  Disabled,
  Invalid,
  AutoResize,
} from './InputTextArea.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudInputTextAreaModule],
    }),
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    },
  }}
  title="@freud-ds/web-components/Forms/InputTextArea"
  component={FreudInputTextAreaComponent}
/>

<FreudHeaderComponent
  title="Input Text Area"
  description="O componente Text Area permite criar facilmente entradas de texto de várias linhas."
/>

## <a id="importing"></a>Import

```ts
import { FreudInputTextAreaModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos

<br />

### Default

<Canvas>
  <Story story={Default} />
</Canvas>

### Invalid

<Canvas>
  <Story story={Invalid} />
</Canvas>

### Disabled

<Canvas>
  <Story story={Disabled} />
</Canvas>

### AutoResize

<Canvas>
  <Story story={AutoResize} />
</Canvas>

### BG Color

<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor} />
</Canvas>

## <a id="api"></a>API

<ArgTypes of={FreudInputTextAreaComponent} />
