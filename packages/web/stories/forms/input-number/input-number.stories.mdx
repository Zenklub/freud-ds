import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudInputNumberComponent, FreudInputNumberModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Default,
  BGColor,
  Disabled,
  ButtonLayout,
  Invalid,
  ErrorText
} from './InputNumber.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudInputNumberModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Forms/InputNumber" component={FreudInputNumberComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Input Number",
      description: "Input com foco em contagem de numerais."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudInputNumberModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### Invalid
<Canvas>
  <Story story={Invalid}/>
</Canvas>

### ErrorText
<Canvas>
  <Story story={ErrorText}/>
</Canvas>

### Disabled
<Canvas>
  <Story story={Disabled}/>
</Canvas>

### ButtonLayout (default, horizontal, vertical)
<Canvas>
  <Story story={ButtonLayout}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor}/>
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudInputNumberComponent} />
