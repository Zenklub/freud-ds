import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudMultiSelectComponent, FreudMultiSelectModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Default,
  BGColor,
  Disabled,
  Invalid,
   Chip
} from './MultiSelect.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudMultiSelectModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Forms/MultiSelect" component={FreudMultiSelectComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Multiselect",
      description: "O MultiSelect permite a pessoa usuária escolha várias opções dentro de uma lista já pré-definida."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudMultiSelectModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### Invalid
<Canvas>
  <Story story={Invalid}/>
</Canvas>

### Disabled
<Canvas>
  <Story story={Disabled}/>
</Canvas>


### Chip
Utilize o parametro `display` com o valor `chip`.
<Canvas>
  <Story story={Chip}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor}/>
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudMultiSelectComponent} />
