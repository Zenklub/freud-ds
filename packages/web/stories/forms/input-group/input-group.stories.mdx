import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudInputTextComponent, FreudInputTextModule, FreudButtonModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  BGColor,
  IconLeft,
  IconRight,
  RadioAndButton,
  IconAndRadio
} from './InputGroup.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudInputTextModule, FreudButtonModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Forms/InputGroup" component={FreudInputTextComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Input Group",
      description: "Texto, ícone, botões e outros conteúdos podem ser agrupados ao lado de input de entrada."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudInputTextModule, FreudButtonModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos
<br />

### IconLeft
<Canvas>
  <Story story={IconLeft}/>
</Canvas>

### IconRight
<Canvas>
  <Story story={IconRight}/>
</Canvas>

### RadioAndButton
<Canvas>
  <Story story={RadioAndButton}/>
</Canvas>

### IconAndRadio
<Canvas>
  <Story story={IconAndRadio}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#6732D1' }}>
  <Story story={BGColor}/>
</Canvas>
