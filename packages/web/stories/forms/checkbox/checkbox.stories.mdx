import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudCheckboxComponent, FreudCheckboxModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Default,
  BGColor,
  Disabled,
  Invalid
} from './Checkbox.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudCheckboxModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Forms/Checkbox" component={FreudCheckboxComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Checkbox",
      description: "É usado em formulários quando um usuário precisa selecionar vários valores de várias opções."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudCheckboxModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### Invalid
<Canvas>
  <Story story={Invalid}/>
</Canvas>

### Disabled
<Canvas>
  <Story story={Disabled}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor}/>
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudCheckboxComponent} />
