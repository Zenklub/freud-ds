import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { GuildCheckboxComponent, GuildCheckboxModule } from '@freud-ds/web-components';
import {
  Default
} from './GuildCheckbox.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [GuildCheckboxModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Forms/GuildCheckbox" component={GuildCheckboxComponent} />

## <a id="importing"></a>Import

```ts
import { GuildCheckboxModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

<ArgsTable of={GuildCheckboxComponent} />
