import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudImageComponent, FreudImageModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Default,
  Preview
} from './Image.stories.ts';

<Meta
  decorators={[
    moduleMetadata({imports: [FreudImageModule]})
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
  title="@freud-ds/web-components/Media/Image"
  component={FreudImageComponent}
/>

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Image",
      description: "Para exibir diferentes tipos de imagens em interfaces."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudImageModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### Preview
<Canvas>
  <Story story={Preview}/>
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudImageComponent} />
