import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudTypographyComponent, FreudTypographyModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../header/header.component';
import { headingTypes } from '../../projects/web-components/src/components/typography/typrography.type';

import { 
  DefaultEditavel,
  HeadingRegular,
  HeadingSemibold,
  HeadingBold,
  BgColor
} from './typography.stories.ts';

<Meta 
  decorators={[
    moduleMetadata({
      imports: [FreudTypographyModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
  argTypes={{
    fontType: {
      control: {
        type: 'select',
        options: headingTypes
      }
    },
    label: {
      name: 'Texto',
      type: { name: 'string', required: false },
      defaultValue: 'Texto',
      description: 'Texto a ser exibido',
      table: {
        type: { summary: 'string' },
        defaultValue: { summary: 'Texto' }
      }
    }
  }}
title="@freud-ds/web-components/Typography/Heading" component={FreudTypographyComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Typography Heading",
      description: "Typography Heading."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudTypographyModule, StepMenu } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos

### Editável
Edite em tempo real pelo canvas
<Canvas>
  <Story story={DefaultEditavel}/>
</Canvas>

### Com background(bgColor)
<Canvas style={{ backgroundColor: '#6732D1' }}>
  <Story story={BgColor}/>
</Canvas>


### Regular
Não permite edição em tempo real pela aba "Canvas"
<Canvas>
  <Story story={HeadingRegular}/>
</Canvas>

### Semibold
Não permite edição em tempo real pela aba "Canvas"
<Canvas>
  <Story story={HeadingSemibold}/>
</Canvas>

### Bold
Não permite edição em tempo real pela aba "Canvas"
<Canvas>
  <Story story={HeadingBold}/>
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudTypographyComponent} />
