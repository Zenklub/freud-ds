import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import {
  FreudPaginatorComponent,
  FreudPaginatorModule,
} from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import { Default, BGColor, PageReport } from './Paginator.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudPaginatorModule],
    }),
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
    },
  }}
  title="@freud-ds/web-components/Others/Paginator"
  component={FreudPaginatorComponent}
/>

<FreudHeaderComponent
  title="Paginator"
  description="É um componente genérico para exibir conteúdo em formato paginado."
/>

## <a id="importing"></a>Import

```ts
import { FreudPaginatorModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos

<br />

### Default

<Canvas>
  <Story story={Default} />
</Canvas>

### PageReport

Utilize a propriedade `currentPageReportTemplate` para montar o template. Os espaços reservados disponíveis são `{currentPage}`, `{totalPages}`, `{rows}`,
`{first}`, `{last}` e `{totalRecords}`.

Atual: `({currentPage} de {totalPages})`

<Canvas>
  <Story story={PageReport} />
</Canvas>

### BG Color

<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor} />
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudPaginatorComponent} />
