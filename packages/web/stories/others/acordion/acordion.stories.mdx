import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import {
  FreudAccordionComponent,
  FreudAccordionModule,
} from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import { Default, BGColor, Multiple, Disabled } from './Acordion.stories.ts';
import { AccordionModule } from 'primeng/accordion';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudAccordionModule, AccordionModule],
    }),
  ]}
  parameters={{
    layout: 'centered',
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    },
  }}
  title="@freud-ds/web-components/Others/Accordion"
  component={FreudAccordionComponent}
/>

<FreudHeaderComponent
  title="Accordion"
  description="Agrupa uma coleção de conteúdos em abas."
/>

## <a id="importing"></a>Import

```ts
import { FreudAccordionModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos

<br />

### Default

<Canvas>
  <Story story={Default} />
</Canvas>

### Multiple

<Canvas>
  <Story story={Multiple} />
</Canvas>

### Disabled

<Canvas>
  <Story story={Disabled} />
</Canvas>

### BG Color

<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor} />
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudAccordionComponent} />
