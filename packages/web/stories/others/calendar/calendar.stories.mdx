import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudCalendarComponent, FreudCalendarModule, FreudButtonModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Default,
  BGColor
} from './Calendar.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudCalendarModule, FreudButtonModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Others/Calendar" component={FreudCalendarComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Calendar",
      description: "Calendário é um componente de entrada para selecionar uma data."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudCalendarModule } from '@freud-ds/web-components';
```

## Getting Started
O card é definido com o elemento `freud-calendar`.
```html
<freud-calendar [(ngModel)]="value"></freud-calendar>
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor}/>
</Canvas>

## <a id="api"></a>API
<ArgsTable of={FreudCalendarComponent} />
