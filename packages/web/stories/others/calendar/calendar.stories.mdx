import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudCalendarComponent, FreudCalendarModule, FreudButtonModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Default,
  BGColor,
  TimeOnly,
  MonthView,
  YearNavigator,
  Input,
  AppendTo
} from './Calendar.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudCalendarModule, FreudButtonModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Others/Calendar" component={FreudCalendarComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Calendar",
      description: "Calendário é um componente de entrada para selecionar uma data."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudCalendarModule } from '@freud-ds/web-components';
```

## Getting Started
O card é definido com o elemento `freud-calendar`.
```html
<freud-calendar ></freud-calendar>
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### Com input
<Canvas>
  <Story story={Input}/>
</Canvas>

### AppendTo
 default: 'body' ( renderiza o calendário no body da página ) pode ser usado com [appendTo]="mydiv" com a div com id="mydiv" 	
<Canvas>
  <Story story={AppendTo}/>
</Canvas>

### TimeOnly
Utilize a propriedade `timeOnly` para exibir somente as horas, e a propriedade `hourFormat` para alternar a visualização
entre `12` e `24` horas
<Canvas>
  <Story story={TimeOnly}/>
</Canvas>

### MonthView
Utilize a propriedade `view` para exibir o calendário em formato de Mês.
<Canvas>
  <Story story={MonthView}/>
</Canvas>

### YearNavigator
<Canvas>
  <Story story={YearNavigator}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor}/>
</Canvas>

## <a id="api"></a>API

### DateFormat
Default date format is dd/mm/yy, to customize this use dateFormat property.

```html
<freud-calendar dateFormat="dd.mm.yy"></freud-calendar>
```
Following options can be a part of the format.

- d - day of month (no leading zero)
- dd - day of month (two digit)
- o - day of the year (no leading zeros)
- oo - day of the year (three digit)
- D - day name short
- DD - day name long
- m - month of year (no leading zero)
- mm - month of year (two digit)
- M - month name short
- MM - month name long
- y - year (two digit)
- yy - year (four digit)
- @ - Unix timestamp (ms since 01/01/1970)
- ! - Windows ticks (100ns since 01/01/0001)
- '...' - literal text
- '' - single quote
- anything else - literal text


<ArgsTable of={FreudCalendarComponent} />
