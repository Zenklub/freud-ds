import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudTableComponent, FreudTableModule, FreudButtonModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Default,
  BGColor,
} from './Table.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudTableModule, FreudButtonModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    },
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Others/Table" component={FreudTableComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Table Basic",
      description: "Requer uma coleção para exibir junto com os componentes da coluna para a representação dos dados."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudTableModule } from '@freud-ds/web-components';
```

## Getting Started
O card é definido com o elemento `freud-table`.
```html
<freud-table>
    Content
</freud-table>
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor}/>
</Canvas>

## <a id="api"></a>API
<ArgsTable of={FreudTableComponent} />
