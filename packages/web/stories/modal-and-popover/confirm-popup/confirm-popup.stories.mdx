import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudConfirmPopupComponent, FreudConfirmPopupModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Default
} from './ConfirmPopup.stories.ts';
import {
  FreudConfirmPopupExampleModule
} from "./confirm-popup-example/example.module";

<Meta
  decorators={[
    moduleMetadata({imports: [FreudConfirmPopupModule, FreudConfirmPopupExampleModule]})
  ]}
  parameters={{
    layout: 'centered',
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    },
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
  title="@freud-ds/web-components/Modal e Popover/ConfirmPopup"
  component={FreudConfirmPopupComponent}
/>

<FreudHeaderComponent
  title="ConfirmPopup"
  description="Exibe uma sobreposição de confirmação exibida relativamente ao seu destino."
/>

## <a id="importing"></a>Import
```ts
import { FreudConfirmPopupModule  } from '@freud-ds/web-components';
```

## Getting Started
FreudConfirmPopup é definido usando a tag freud-confirm-popup e uma instância de FreudConfirmationService é necessária para exibi-lo chamando o método confirm.
```html
<freud-confirm-popup></freud-confirm-popup>
<freud-button (click)="confirm($event)">Clique aqui!</freud-button>
```
```ts
import { FreudConfirmationService  } from '@freud-ds/web-components';

export class ConfirmDialogDemo {

    constructor(private confirmationService: FreudConfirmationService) {}

    confirm(event) {
        this.confirmationService.confirm({
            target: event.target,
            message: 'Mensagem.',
            acceptLabel: 'Aceitar',
            rejectLabel: 'Rejeitar',
            accept: () => {
                //Ação quando o usuário aceitar
            },
            reject: () => {
                // Ação quando o usuário rejeitar
            }
        });
    }
}
```

## <a id="exemplos"></a>Exemplo
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>
