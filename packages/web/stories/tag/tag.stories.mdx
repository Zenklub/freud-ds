import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudTagComponent, FreudTagModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../header/header.component';
import {
  Primary,
  Success,
  Info,
  Warning,
  Error, OutlinePrimary, OutlineSuccess, OutlineInfo, OutlineWarning, OutlineError, BgColor, BgColorOutLine,
} from './Tag.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudTagModule],
    })
  ]}
  argTypes={{
    color: {
      control: {
        type: 'select',
        options: ["primary", "success", "info", "warning", "error"]
      },
    }
  }}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
  title="@freud-ds/web-components/Feedback/Tag"
  component={FreudTagComponent}
/>

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Tag",
      description: "São usados ​​para destacar o status de um item para reconhecimento rápido."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import FreudTagModule from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos
<br />

### Cores
<Canvas>
  <Story story={Primary}/>
  <Story story={Success}/>
  <Story story={Info}/>
  <Story story={Warning}/>
  <Story story={Error}/>
</Canvas>

### Outline
<Canvas>
  <Story story={OutlinePrimary}/>
  <Story story={OutlineSuccess}/>
  <Story story={OutlineInfo}/>
  <Story story={OutlineWarning}/>
  <Story story={OutlineError}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#3B1C78' }}>
  <Story story={BgColor}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#3B1C78' }}>
  <Story story={BgColorOutLine}/>
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudTagComponent} />
