import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudProgressSpinnerComponent, FreudProgressSpinnerModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../header/header.component';
import {
  Default,
  BgColor,
} from './ProgressSpinner.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudProgressSpinnerModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    },
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Feedback/ProgressSpinner" component={FreudProgressSpinnerComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "ProgressSpiner",
      description: "ProgressSpinner é um indicador de status do processo."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudRatingComponent } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#3B1C78' }}>
  <Story story={BgColor}/>
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudProgressSpinnerComponent} />
