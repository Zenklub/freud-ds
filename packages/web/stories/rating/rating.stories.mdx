import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudRatingComponent, FreudRatingModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../header/header.component';
import {
  Default,
  Disabled,
  Readonly,
  BgColor,
  BgColorDisabled,
  CancelButton
} from './Rating.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudRatingModule],
    })
  ]}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Feedback/Rating(Em andamento)" component={FreudRatingComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Rating",
      description: "O componente de classificação é uma entrada de seleção baseada em estrelas."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudRatingComponent } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### Disabled
<Canvas>
  <Story story={Disabled}/>
</Canvas>

### Readonly
<Canvas>
  <Story story={Readonly}/>
</Canvas>

### Com background(bgColor)
<Canvas style={{ backgroundColor: '#3B1C78' }}>
  <Story story={BgColor}/>
  <Story story={BgColorDisabled}/>
</Canvas>

### Com botão de cancelar
Ao cancelar é emitido um output no output `onCancel`
<Canvas>
  <Story story={CancelButton}/>
</Canvas>


## <a id="api"></a>API

<ArgsTable of={FreudRatingComponent} />
