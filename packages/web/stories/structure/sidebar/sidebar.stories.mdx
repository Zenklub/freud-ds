import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudSidebarComponent, FreudSidebarModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import {
  Left,
  Right,
  Top,
  Down,
  FullScreen, 
  Modal
} from './sidebar.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudSidebarModule]
    })
  ]}
  parameters={{
    layout: 'centered',
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true }
    },
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'Right', value: '#FFFFFF' },
        { name: 'Top', value: '#FFFFFF' },
        { name: 'Down', value: '#FFFFFF' },
        { name: 'FullScreen', value: '#FFFFFF' },
        { name: 'Modal', value: '#FFFFFF' },
      ],
    }
  }}
  title="@freud-ds/web-components/Structure/Sidebar"
  component={FreudTSidebarComponent}
/>

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Sidebar",
      description: "É um componente de composição de interface para informações extras."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudSidebarComponent } from '@freud-ds/web-components';
```

## Getting Started
```html
      <freud-sidebar></freud-sidebar>
```

## <a id="exemplos"></a>Exemplos
<br />

### Default
Padrão tem posicionamento a esquerda
<Canvas>
  <Story story={Default}/>
</Canvas>

### Right
<Canvas>
  <Story story={Right}/>
</Canvas>

### Top
<Canvas>
  <Story story={Top}/>
</Canvas>

### Down
<Canvas>
  <Story story={Down}/>
</Canvas>

### Modal
<Canvas>
  <Story story={Modal}/>
</Canvas>

### FullScreen
<Canvas>
  <Story story={FullScreen}/>
</Canvas>


## <a id="api"></a>API

<ArgsTable of={FreudSidebarComponent} />
