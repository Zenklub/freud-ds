import { Meta, Canvas, Story, ArgTypes } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import {
  FreudScrollPanelComponent,
  FreudScrollPanelModule,
} from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../../header/header.component';
import { Default, BGColor } from './ScrollPanel.stories.ts';

<Meta
  decorators={[moduleMetadata({ imports: [FreudScrollPanelModule] })]}
  parameters={{
    layout: 'centered',
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
  title="@freud-ds/web-components/Structure/ScrollPanel"
  component={FreudScrollPanelComponent}
/>

<FreudHeaderComponent
  title="ScrollPanel"
  description="ScrollPanel é um navegador cruzado, alternativa leve e programável para a barra de rolagem nativa do navegador."
/>

## <a id="importing"></a>Import

```ts
import { FreudScrollPanelModule } from '@freud-ds/web-components';
```

## <a id="exemplos"></a>Exemplos

<br />

### Default

<Canvas>
  <Story story={Default} />
</Canvas>

### BG Color

<Canvas style={{ backgroundColor: '#241249' }}>
  <Story story={BGColor} />
</Canvas>

## <a id="api"></a>API

<ArgTypes of={FreudScrollPanelComponent} />
