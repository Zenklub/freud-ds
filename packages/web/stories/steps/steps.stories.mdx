import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { RouterTestingModule } from '@angular/router/testing';
import { FreudStepsComponent, FreudStepsModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../header/header.component';
import {
  Default,
  BgColor
} from './Steps.stories.ts';

<Meta
  decorators={[
    moduleMetadata({
      imports: [FreudStepsModule, RouterTestingModule],
    })
  ]}
  parameters={{
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
title="@freud-ds/web-components/Feedback/Steps" component={FreudStepsComponent} />

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Steps",
      description: "Componentes de etapas é um indicador para as etapas em um fluxo de trabalho."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import { FreudStepsModule, StepMenu } from '@freud-ds/web-components';
```

### Declaração dos itens
```ts
const itens: StepMenu[] = [
  {label: 'Item1'},
  {label: 'Item2'},
  {label: 'Item3'},
  {label: 'Item4'}
]
```

## <a id="exemplos"></a>Exemplos

### Default
<Canvas>
  <Story story={Default}/>
</Canvas>

### BG Color
<Canvas style={{ backgroundColor: '#3B1C78' }}>
  <Story story={BgColor}/>
</Canvas>

## <a id="api"></a>API

<ArgsTable of={FreudStepsComponent} />
