import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import { moduleMetadata } from '@storybook/angular';
import { FreudAvatarComponent, FreudAvatarModule } from '@freud-ds/web-components';
import { FreudHeaderComponent } from '../header/header.component';
import { 
  Small,
  Medium,
  Large,
  BgColor,
  Imagem,
  ImagemEBadge,
  BadgeSmall,
  BadgeMedium,
  BadgeLarge
} from './Avatar.stories.ts';

<Meta 
  decorators={[
    moduleMetadata({
      imports: [FreudAvatarModule],
    })
  ]}
  argTypes={{
    size: {
      control: {
        type: 'select',
        options: ["sm", "md", "lg"]
      },
    }
  }}
  parameters={{
    layout: 'centered',
    backgrounds: {
      default: 'Default',
      values: [
        { name: 'Default', value: '#FFFFFF' },
        { name: 'BgColor', value: '#6732D1' },
      ],
    }
  }}
  title="@freud-ds/web-components/Media/Avatar"
  component={FreudAvatarComponent}
/>

<Story name="Storyname-hidden">
  {{
    component: FreudHeaderComponent,
    props: {
      title: "Avatar",
      description: "É normalmente usado para exibir imagens de perfil de usuário."
    },
  }}
</Story>

## <a id="importing"></a>Import

```ts
import FreudAvatarModule from '@freud-ds/web-components';
```

## <a id="exemplosw"></a>Exemplos

#### Tamanhos
<Canvas>
  <Story story={Small}/>
  <Story story={Medium}/>
  <Story story={Large}/>
</Canvas>

#### Com background
<Canvas style={{ backgroundColor: '#6732D1' }}>
  <Story story={BgColor}/>
</Canvas>

#### Imagem
<Canvas>
  <Story story={Imagem}/>
  <Story story={ImagemEBadge}/>
</Canvas>

#### Com icone
<Canvas>
  Aguardando a finalização do componente de icones
</Canvas>

#### Com badge
<Canvas>
  <Story story={BadgeSmall}/>
  <Story story={BadgeMedium}/>
  <Story story={BadgeLarge}/>
</Canvas>


## <a id="api"></a>API

<ArgsTable of={FreudAvatarComponent} />
