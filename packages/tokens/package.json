{
  "name": "@freud-ds/tokens",
  "version": "2.0.1",
  "description": "Zenklub - Freud DS Tokens",
  "author": "Zenklub",
  "homepage": "",
  "license": "ISC",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && node ./build.js && npm run copy",
    "copy": "cp -r assets dist/ && cp package.json dist/package.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "style-dictionary": "3.7.0"
  }
}
