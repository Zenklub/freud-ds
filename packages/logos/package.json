{
  "name": "@freud-ds/logos",
  "version": "2.0.0",
  "description": "Zenklub - Freud DS Logos",
  "author": "Zenklub",
  "homepage": "",
  "license": "ISC",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && cp -r assets ./dist && npm run copy",
    "copy": "cp package.json dist/package.json && cp README.md dist/README.md",
    "clean": "rm -rf dist"
  }
}
