{
    "name": "feud-tokens",
    "version": "1.0.0",
    "description": "Zenklub Feud tokens",
    "main": "build/index.js",
    "files": [
      "build"
    ],
    "scripts": {
      "build": "npm run clean && node_modules/.bin/style-dictionary build && npm run copy",
      "clean": "rm -rf build",
      "copy": "cp -r assets build/",
      "prepublishOnly": "npm run build",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Zenklub",
    "license": "Apache-2.0",
    "devDependencies": {
      "style-dictionary": "3.7.0"
    }
  }