.docker-image-builder-template:
  stage: docker images
  image:
    name: "${KANIKO_EXECUTOR_IMAGE}"
    entrypoint: [""]

  retry: 2
  script:
    - mkdir -p /kaniko/.docker
    - echo "{\"auths\":{\"${CI_REGISTRY}\":{\"auth\":\"$(printf "%s:%s" "${CI_REGISTRY_USER}" "${CI_REGISTRY_PASSWORD}" | base64 | tr -d '\n')\"}}}" > /kaniko/.docker/config.json
    - >-
      /kaniko/executor
      --context "${CI_PROJECT_DIR}"
      --dockerfile "${CI_PROJECT_DIR}/docker/${IMAGE_NAME}/Dockerfile"
      --destination "${CI_REGISTRY_IMAGE}/${IMAGE_NAME}:latest"


image packages builder:
  extends: .docker-image-builder-template
  variables:
    IMAGE_NAME: freud-builder
  rules:
    - changes:
        - docker/${IMAGE_NAME}/Dockerfile
      when: always
      allow_failure: true
    - when: manual
